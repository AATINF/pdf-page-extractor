# 🛠 开发指南（代码复刻与使用）

本文面向开发者，说明项目结构、技术栈、构建流程，以及如何基于现有代码复刻或扩展功能。普通用户请看 [教程.html](教程.html)。

## 1. 项目结构

```
pdf-page-extractor/
├── PDF页面提取器-单文件版.html   # 自包含单文件版（约 2MB，离线即用）
├── index.html                    # 多文件版入口
├── app.js                        # 核心业务逻辑
├── manifest.webmanifest          # PWA 清单
├── sw.js                         # Service Worker（离线缓存）
├── serve.py                      # 局域网服务器（本地测试用）
├── publish.sh                    # 版本同步 + 推送到 GitHub
├── icons/                        # PWA 图标
├── lib/                          # 第三方库（pdf.js / pdf-lib / jszip）
├── tools/                        # 构建脚本
│   ├── build.py                  # 内联 PDF.js worker 到 index.html
│   ├── build_single.py           # 生成单文件版
│   └── make_icons.py             # 生成图标
├── 教程.html                     # 用户使用教程
├── README.md / README.en.md      # 文档
├── CHANGELOG.md                  # 版本历史
└── LICENSE
```

## 2. 技术栈与许可证

| 组件 | 版本 | 许可证 | 用途 |
|---|---|---|---|
| PDF.js | 3.11.174 | Apache-2.0 | PDF 解析、渲染、缩略图 |
| pdf-lib | 1.17.1 | MIT | 页面复制 / 旋转 / 生成 |
| JSZip | 3.10.1 | MIT | 单页拆分 ZIP 打包 |

> ⚠️ 重分发时务必保留 `lib/` 各文件头部的版权声明。

## 3. 核心数据流

1. **加载**：PDF.js 解析用户所选 PDF，逐页渲染缩略图。
2. **选择**：用户勾选页面或输入范围（如 `2-5,8`）。
3. **操作**：用 pdf-lib 复制选中页、应用旋转、调整顺序；如需图片化（加密 PDF）则先用 PDF.js 将页渲染为图片再嵌入。
4. **生成 / 下载**：pdf-lib 合成新 PDF（合并场景多份合成）；拆分场景用 JSZip 打包为 ZIP。

要点：**PDF.js 负责“看”，pdf-lib 负责“写”**，所有加工都在浏览器内存完成，不依赖任何服务器。

## 4. 构建

```bash
python3 tools/build.py          # 将 PDF.js worker 内联进 index.html
python3 tools/build_single.py   # 生成单文件版 HTML
python3 tools/make_icons.py     # 生成 PWA 图标
```

> 若只改了 `lib/` 里的库，需重跑 `build.py` 让 `index.html` 重新内联。

## 5. 本地运行

```bash
cd pdf-page-extractor
python3 serve.py            # 局域网服务器，手机可访问
# 或
python3 -m http.server 8000
```

浏览器打开对应地址即可。单文件版可直接双击 `PDF页面提取器-单文件版.html`。

## 6. 如何复刻 / 扩展一个功能（示例：一键删除空白页）

1. 在 `app.js` 中找到“提取为 PDF”的入口函数（处理用户选中页数组的地方）。
2. 新增判断函数 `isBlankPage(page)`：用 PDF.js 将页渲染到离屏 canvas，统计非白像素比例，低于阈值视为空白。
3. 在提交逻辑前插入：`pages = pages.filter(p => !isBlankPage(p))`。
4. 在界面上加一个“跳过空白页”复选框，绑定该逻辑。
5. 本地 `python3 serve.py` 验证，再 `python3 tools/build_single.py` 重新生成单文件版。

核心思路始终是：**解析用 PDF.js，写出用 pdf-lib，打包用 JSZip**，无后端参与。

## 7. 发布

版本更新用 `publish.sh`：

```bash
./publish.sh v1.0.1 "本次更新说明"
```

它会同步版本号到 `index.html` 标题 / 版本标签 / `sw.js` 缓存名 / `README`，做 JS 语法检查，然后提交并推送到 GitHub。
